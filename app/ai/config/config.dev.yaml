# Environment overrides for Development
app:
  environment: "dev"

tools:
  write_operations:
    enabled: true
    require_confirmation: true

mcp:
  servers:
    incidents:
      url: "http://127.0.0.1:8080/mcp"

development:
  debug: true
  verbose_errors: true


